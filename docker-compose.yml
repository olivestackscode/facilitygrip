services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=sqlite:///./facilitygrip.db

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
